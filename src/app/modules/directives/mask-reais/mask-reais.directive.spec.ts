import { MaskReaisDirective } from './mask-reais.directive';

describe('MaskReaisDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskReaisDirective();
    expect(directive).toBeTruthy();
  });
});
